export interface User {
    [key: string]: any
};

export interface UrlParams {
    [key: string]: string;
};

export interface Obj {
    [key: string]: any
};

export interface EBState {
    hasError: Boolean;
};
